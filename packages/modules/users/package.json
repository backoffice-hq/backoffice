{
    "name": "@backoffice/users",
    "version": "0.0.1",
    "private": true,
    "main": "./src/index.ts",
    "types": "./src/index.ts",
    "scripts": {
        "build": "rimraf dist && tsup",
        "dev": "tsup --watch",
        "lint": "eslint \"src/**/*.ts*\"",
        "clean": "rimraf .turbo && rimraf node_modules && rimraf dist"
    },
    "dependencies": {
        "@backoffice/core": "workspace:*",
        "@backoffice/database": "workspace:*"
    },
    "devDependencies": {
        "@backoffice/eslint-config": "workspace:*",
        "@backoffice/typescript-config": "workspace:*",
        "@types/node": "^20",
        "rimraf": "^6.0.1",
        "tsup": "^8.0.2",
        "typescript": "^5.7.3"
    }
}